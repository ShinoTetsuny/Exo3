<style>
    nav {
        background: linear-gradient(to left, #8a2be2, #ff4500);
        box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
    }

    body {
        margin-top: 50px;
    }

    ul {
        list-style: none;
        padding: 0;
        margin: 0;
        display: flex;
        justify-content: flex-start;
    }

    li {
        margin-right: 20px;
    }

    a {
        text-decoration: none;
        color: #fff;
        font-weight: bold;
    }
</style>

<nav>
    <ul>
        <li><a href="{{ path('app_grade') }}">{{ 'navbar.home'|trans }}</a></li>
        <li><a href="{{ path('app_subject') }}">{{ 'navbar.subject'|trans }}</a></li>
    </ul>
</nav>
